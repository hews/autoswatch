from setuptools import setup

setup(
    name="autoswatch",
    packages=["autoswatch"],
    include_package_data=True,
    install_requires=[
        "flask",
    ],
    setup_requires=[
    ],
    tests_require=[
    ],
)

